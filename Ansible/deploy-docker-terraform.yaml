---
- name: Wait for ssh connection 
  hosts: all
  gather_facts: False
  tasks:
    - name: Ensure ssh port open
      wait_for:
        port: 22
        delay: 10
        timeout: 100
        search_regex: OpenSSH
        host: '{{ (ansible_ssh_host|default(ansible_host))|default(inventory_hostname) }}'
      vars:
        ansible_connection: local

- name: Install Docker
  hosts: all
  become: yes
  tasks:
    - name: Install docker
      yum:
        name: docker
        update_cache: yes
        state: present

- name: Install Docker_compose
  hosts: all
  become: yes
  tasks:
    - name: Install Docker-compose 
      get_url: 
        url: https://github.com/docker/compose/releases/download/v2.29.1/docker-compose-linux-{{lookup('pipe', 'uname -m')}}
        dest: /usr/local/bin/docker-compose
        mode: +x


- name: Start docker
  hosts: all
  become: yes
  tasks:
    - name: Start docker daemon
      systemd:
        name: docker
        state: started

# - name: Install docker python module
#   pip:
#     name: docker

- name: Add user to docker group
  hosts: all
  become: yes
  tasks:
    - name: add ec2-user to docker group
      user:
        name: ec2-user
        groups: docker
        append: yes
    # we use this to exit and reconnect to server for group update to take place
    - name: Reconnect to server session
      meta: reset_connection
 
# - name: Test dokcer pull 
#   hosts: all
#   tasks:
#     - name: Pull redis 
#       docker_image:
#         name: redis
#         source: pull

- name: Start docker containers
  hosts: all
  vars_files:
    - docker-vars

  tasks:
    - name: Copy docker-compose file
      copy:
        src: ./docker-compose.yaml
        dest: /home/ec2-user/docker-compose.yaml

    - name: Docker login
      community.docker.docker_login:
        registry_url: https://index.docker.io/v1/
        username: "{{ docker_username }}"
        password: "{{ docker_password }}"

    - name: Start containers using docker-compose
      command: docker-compose up -d
      args:
        chdir: /home/ec2-user

# - name: Start docker containers
#   hosts: all
#   vars_files:
#     - docker-vars
#   # vars_prompt:
#   #   - name: docker_username
#   #     prompt: Enter username for docker
#   #   - name: docker_password
#   #     prompt: Enter password for docker
#   tasks:
#     - name: Copy docker-compose file
#       copy:
#         src: ./docker-compose.yaml
#         dest: /home/ec2-user/docker-compose.yaml

#     - name: Docker login
#       docker_login:
#         registry_url: https://index.docker.io/v1
#         username: "{{ docker_username }}"
#         password: "{{ docker_password }}"
#     - name: Start containers
#       docker_compose:
#         project_src: /home/ec2-user
#         state: present 
#         ##absent for down